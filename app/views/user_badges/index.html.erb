<div class="container-fluid">
  <div class="row">
    <div class="col-12 text-center">
      <h1><%= @user.email %></h1>
    </div>
  </div>

  <div class="row" id="badges-cards">
    <div class="col-12">
      <p>badges :</p>
    </div>
    <% @userbadges.each do |userbadge| %>
      <div class="col-12 mb-3">
        <%= link_to user_badge_path(userbadge), class: "card border-left-0" do %>
          <div class="card-body">
            <h4 class="card-title"><%= userbadge.badge.name %></h4>
            <p class="card-text"><%=  %></p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
